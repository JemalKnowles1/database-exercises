CREATE TABLE albums2 (
                        id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
                        artist VARCHAR(255),
                        title VARCHAR(255),
                        release_date INT,
                        sales FLOAT,
                        genre VARCHAR(255)
);

